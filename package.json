{
  "name": "fovea",
  "private": true,
  "version": "0.1.0",
  "description": "Weekly brain-map project manager — the week has one sharpest point of focus",
  "license": "MIT",
  "workspaces": [
    "client",
    "server",
    "mobile"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w fovea-server\" \"npm run dev -w fovea-client\"",
    "dev:mobile": "concurrently -n server,client,expo -c cyan,magenta,green \"npm run dev -w fovea-server\" \"npm run dev -w fovea-client\" \"npm run start -w fovea-mobile\"",
    "install:all": "npm install",
    "build": "npm run build -w fovea-client",
    "start": "node server/src/index.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.1.2"
  }
}
